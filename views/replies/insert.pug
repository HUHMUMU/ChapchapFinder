form.p-4.border(method="post", action="/replies/insert.do,enctype=\"multipart/form-data")
    div.w-100
        input(name="review_num",value=review.review_num,hidden=true)
        p.text-end.h4 사장 댓글 등록 폼
        div.row
            div.form-floating.col
                input.form-control(name="rr_num",value="")
                label 댓글 번호
            div.form-floating.col
                input.form-control(name="store_name",value=store.store_name)
                label 가게 이름:
        div.form-floating
            textarea#inputText.form-control(name="content")
            label 내용
        div.mt-2.text-end
            button.btn.btn-outline-warning.me-2(type="reset") 초기화
            button#submitBtn.btn.btn-outline-primary(type="submit") 등록



